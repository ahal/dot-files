# VCS

* If `.jj` is preset at the top of the repo, use `jj` and never use git, or we'll
make a mess.
* Never create new commits, leave changes in the working directory instead.

# Style

* Remove trailing whitespace

## Python

* Imports should go at top of files or linters will complain.
